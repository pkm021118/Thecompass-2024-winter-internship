import React from 'react';

const ProjectDetail = ({ project }) => {
    return (
        <div>
            <h2>{project.name}</h2>
            {/* 추가 정보 표시 */}
        </div>
    );
};

export default ProjectDetail;
